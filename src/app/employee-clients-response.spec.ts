import { EmployeeClientsResponse } from './employee-clients-response';

describe('EmployeeClientsResponse', () => {
  it('should create an instance', () => {
    expect(new EmployeeClientsResponse()).toBeTruthy();
  });
});
