import { ClientEmployeesResponse } from './client-employees-response';

describe('ClientEmployeesResponse', () => {
  it('should create an instance', () => {
    expect(new ClientEmployeesResponse()).toBeTruthy();
  });
});
